# Frontend (Vite + React + TypeScript)

Current frontend scope is intentionally minimal:

- A clean tweet text box
- A Post button (no backend action yet)

## Run Frontend

```bash
cd frontend
npm install
npm run dev
```

Open http://localhost:5173 in your browser.

## Build Frontend

```bash
npm run build
```
